# 2014/05/08 
# NLAN Modules testing
#
  - do: init2.yaml
  - comment: 'add _name'
    nlan:
        router: rpi1
        options: --add
        args:
            services:
                - _index: [name, snort1]
                  name: snort1
  - comment: 'add _name (lxc-ls)'
    nlan:
        options: --raw
        args: 'lxc-ls'
  - comment: 'add _chain'
    nlan:
        options: --add
        args:
            services:
                - _index: [name, snort1]
                  chain:
                    - aaa.101
                    - bbb.102
  - comment: 'add _chain (lxc-ls)'
    nlan:
        options: --raw
        args: 'lxc-ls'


